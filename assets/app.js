function toggleSidebar(){document.body.classList.toggle('sidebar-open')}
function bindAttendanceCounters(){const container=document.querySelector('[data-attendance-grid]');if(!container)return;const updateCounts=()=>{const presentChecked=container.querySelectorAll('input[type="checkbox"][data-status="present"]:checked').length;const absentChecked=container.querySelectorAll('input[type="checkbox"][data-status="absent"]:checked').length;const presentEl=document.querySelector('[data-present-count]');const absentEl=document.querySelector('[data-absent-count]');if(presentEl)presentEl.textContent=presentChecked;if(absentEl)absentEl.textContent=absentChecked};container.addEventListener('change',(event)=>{if(event.target.matches('input[type="checkbox"]')){const row=event.target.closest('tr');if(row&&event.target.dataset.status==='present'&&event.target.checked){const absent=row.querySelector('input[data-status="absent"]');if(absent)absent.checked=false}if(row&&event.target.dataset.status==='absent'&&event.target.checked){const present=row.querySelector('input[data-status="present"]');if(present)present.checked=false}updateCounts()}});document.querySelector('[data-mark-all-present]')?.addEventListener('click',()=>{container.querySelectorAll('input[data-status="present"]').forEach(cb=>cb.checked=true);container.querySelectorAll('input[data-status="absent"]').forEach(cb=>cb.checked=false);updateCounts()});updateCounts()}
function deadlineLockdown(){const submitBtn=document.querySelector('[data-submission-submit]');const deadlineEl=document.querySelector('[data-deadline]');if(!submitBtn||!deadlineEl)return;const deadline=new Date(deadlineEl.dataset.deadline).getTime();const timer=()=>{const now=Date.now();if(now>=deadline+60000){submitBtn.disabled=true;submitBtn.textContent='Submission Closed';const note=document.querySelector('[data-deadline-note]');if(note)note.textContent='The deadline has passed. Submission is closed.';clearInterval(intervalId)}};const intervalId=setInterval(timer,1000);timer()}
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-toggle-sidebar]').forEach(btn=>btn.addEventListener('click',toggleSidebar));bindAttendanceCounters();deadlineLockdown()});
